# پروژه کنترل سنسور و لامپ با ESP و کاتلین

## 📱 پروژه اندروید کنترل IoT

یک اپلیکیشن اندروید ساخته شده با کاتلین که با دستگاه ESP ارتباط برقرار می‌کند، داده‌های سنسور را دریافت کرده و لامپ را کنترل می‌کند.

## ✨ ویژگی‌ها

- 📊 نمایش داده‌های سنسور از ESP در زمان واقعی
- 💡 کنترل لامپ (روشن/خاموش)
- 📱 رابط کاربری ساده و کاربرپسند
- 🔄 ارتباط پایدار با دستگاه ESP
- 🌐 پشتیبانی از Wi-Fi
- 🎨 طراحی مدرن Material Design

## 🛠️ تکنولوژی‌های استفاده شده

### Backend (ESP)
- میکروکنترلر ESP32/ESP8266
- PlatformIO / Arduino Framework
- WiFi و HTTP Server
- سنسور (DHT22, DS18B20 یا هر سنسور دیگر)

### Frontend (Android)
- Kotlin
- Android SDK (حداقل API 21)
- Retrofit برای ارتباط شبکه
- Coroutines برای عملیات ناهمزمان
- ViewModel و LiveData
- Material Components

## 📁 ساختار پروژه

```
project/
├── android-app/          # پروژه اندروید
│   ├── app/
│   │   ├── src/main/
│   │   │   ├── java/com/example/iotcontroller/
│   │   │   │   ├── data/
│   │   │   │   │   ├── ApiService.kt
│   │   │   │   │   └── SensorData.kt
│   │   │   │   ├── ui/
│   │   │   │   │   ├── MainActivity.kt
│   │   │   │   │   └── viewmodel/
│   │   │   │   └── utils/
│   │   │   └── res/
│   │   └── build.gradle
│   └── README.md
│
├── esp-code/             # کد ESP
│   ├── src/
│   │   └── main.cpp
│   ├── include/
│   └── platformio.ini
│
└── README.md
```

## 🚀 راه‌اندازی

### پیش‌نیازها

1. **برای اندروید:**
   - Android Studio Arctic Fox یا بالاتر
   - JDK 11
   - Android SDK

2. **برای ESP:**
   - PlatformIO یا Arduino IDE
   - کتابخانه‌های لازم (WiFi, WebServer, سنسور)

### نصب و اجرا

#### قسمت ESP:

1. کد ESP را در پوشه `esp-code` باز کنید
2. تنظیمات WiFi را در کد قرار دهید:
```cpp
const char* ssid = "نام WiFi شما";
const char* password = "رمز WiFi شما";
```

3. کد را بر روی ESP آپلود کنید
4. آدرس IP اختصاص داده شده به ESP را یادداشت کنید

#### قسمت اندروید:

1. پروژه را در Android Studio باز کنید
2. فایل `ApiService.kt` را باز کرده و آدرس IP ESP را تنظیم کنید:
```kotlin
private const val BASE_URL = "http://192.168.1.100" // آدرس IP ESP
```

3. پروژه را build کنید
4. روی دستگاه فیزیکی یا شبیه‌ساز اجرا کنید

## 🔌 API Endpoints

دستگاه ESP باید endpoints زیر را ارائه دهد:

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/sensor` | GET | دریافت داده‌های سنسور |
| `/light` | POST | کنترل لامپ |

### نمونه Response سنسور:
```json
{
  "temperature": 25.5,
  "humidity": 60.0,
  "timestamp": "2024-01-01T12:00:00"
}
```

### نمونه Request کنترل لامپ:
```json
{
  "state": "on"  // یا "off"
}
```


---

⭐ اگر این پروژه را دوست داشتید، به آن ستاره بدید!
